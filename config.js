const { db } = require('./lib/database');

const fs = require('fs')
const path = require('path');

//global.prefix = ['#', '/', '.', '!', '?']

global.owner = []
global.staff = []
global.evaluate = []